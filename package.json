{
  "name": "supabase-plugin-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test harness for the Supabase multi-vendor plugin distribution repo.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run tests/unit",
    "test:install": "vitest run tests/integration",
    "test:all": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "plugins": "1.3.4",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
